# Dependencies (only used to generate icons; not needed to run the app)
node_modules/
package-lock.json
npm-debug.log*

# Local tooling
.claude/
serve.json

# Draft / working documents — not for public publication unless approved
*.docx
resources/Inception Report Draft.docx

# OS / editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Secrets — never commit the real AI key (only the .sample template is tracked)
bbi-secret.php
backend/bbi-secret.php

# Build / packaging output
*.apk
*.aab
*.keystore
signing-key-info.txt
